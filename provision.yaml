#!/usr/bin/env ansible-playbook

- name: Provision an NFS server
  hosts: all
  become: true

  roles:
    # Install and Setup the NFS Server
    - nfs-server
    # Export the specified directories TODO look is we need to extend this role to include the var file
    # - nfs-exports
    # Setup firewall and allow only necessary services through
    - firewall