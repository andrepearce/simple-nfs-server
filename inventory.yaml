# Ansible Inventory
#
# Specified here so we are not dependent on having it exist in /etc/ansible/hosts.
---
all:
  hosts:
    nfs_server:
      ansible_host: 172.16.82.213
      ansible_connection: ssh
      ansible_user: root
      ansible_ssh_private_key_file: ssh-keys/nfs-server